<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/GBText</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Text">
        <item name="android:letterSpacing">0.1</item>
    </style>

    <style name="ListLineCheck">
        <item name="android:background">@color/GBText</item>
        <item name="android:textColor">@color/GBScreen</item>
        <item name="android:fontFamily">@font/game_boy</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="ListLineUncheck">
        <item name="android:background">@color/Transparent</item>
        <item name="android:textColor">@color/GBText</item>
        <item name="android:fontFamily">@font/game_boy</item>
        <item name="android:textSize">10sp</item>
    </style>

</resources>
